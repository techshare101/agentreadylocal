AGENTREADY LOCAL — LIVE EVIDENCE SAMPLE
ARL-20260910-GR8SKIN-001

Public evidence sample: one branded Google AI Overview response and markup inspection of the homepage, contact page, and services page. This is not a completed 100-point audit.

Read manifest.json for each finding, evidence file, source URL, observation time, and limitation. Pending engine tests have no response image and do not count as failures. No 100-point score is assigned.

PNG screenshots are pixel conversions from actual browser JPEG captures. The Google image is cropped to exclude account controls; its query and complete expanded answer remain visible. Crop coordinates and public-file hashes are recorded. Original uncropped browser files and private session context are excluded. Timestamps are observed wall-clock UTC, not independent timestamp attestations.

Schema checks: one homepage WebSite block parses as valid JSON. Contact and services snapshots contain no JSON-LD. This is an inventory/syntax check, not Schema.org certification. The raw full-page HTML is not redistributed; its SHA-256 is retained, with the exact JSON-LD and relevant metadata in schema-check.json.

Verification: compare the member file sizes and SHA-256 values against manifest.json. The manifest does not hash itself. The ZIP checksum is supplied separately to avoid a circular hash.

No endorsement or client relationship is implied. Do not claim improved ranking, citations, appointments, or revenue from this sample.
